# Legacy / reference code

These files are preserved from the supplied VisionFlux/continual-learning code for comparison and future feature extraction. They are **not imported by the production web stack**.

Production inference uses `backend/vendor/sem_fiber_ai`, extracted from the supplied `sem_fiber_ai_colab_v6_11` notebook. The embedded package itself reports internal version `6.6.0`; the notebook revision and the package version are therefore intentionally both documented rather than conflated.
