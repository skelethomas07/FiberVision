# sem_fiber_ai v7.0.0 - pinned ranges (the notebook installs exactly these)
numpy>=1.24,<3
scipy>=1.10,<2
pandas>=2.0,<4
opencv-python-headless>=4.8,<5
scikit-image>=0.21,<0.27
scikit-learn>=1.3,<1.9
matplotlib>=3.7,<4
PyYAML>=6.0,<7
torch>=2.0            # Colab supplies the CUDA build; do not reinstall it
tqdm>=4.65,<5
pytest>=7.4,<10
pytesseract>=0.3.10,<0.4   # optional: reads the burned-in SEM footer for nm/px
# timm is optional and only used by the 'large' model preset
